# GCG Parameters {#PARAMETERS}

This page lists all parameters of the current GCG version. \n
Since GCG is built on top of SCIP, the SCIP commands are also included.

\verbinclude resourcesmiscparameters.set
